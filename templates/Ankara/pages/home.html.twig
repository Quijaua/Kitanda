{% block content %}
    {# ----------- Modais de Sucesso / Erro ----------- #}
    <div class="modal modal-blur fade" id="modal-success" tabindex="-1" role="dialog" aria-hidden="true">
        <div class="modal-dialog modal-sm modal-dialog-centered" role="document">
            <div class="modal-content">
                <button type="button" class="btn-close" data-bs-dismiss="modal"
                        aria-label="Fechar modal"></button>
                <div class="modal-status bg-success"></div>
                <div class="modal-body text-center py-4">
                    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24"
                        viewBox="0 0 24 24" fill="none" stroke="currentColor"
                        stroke-width="2" stroke-linecap="round" stroke-linejoin="round"
                        class="icon mb-2 text-green icon-lg">
                        <path d="M12 12m-9 0a9 9 0 1 0 18 0a9 9 0 1 0 -18 0" />
                        <path d="M9 12l2 2l4 -4" />
                    </svg>
                    <h3>Salvo com sucesso!</h3>
                    <div id="modalMsg" class="text-secondary"></div>
                </div>
                <div class="modal-footer">
                    <div class="w-100">
                        <div class="row">
                            <div class="col">
                                <a href="#" class="btn btn-3 w-100" data-bs-dismiss="modal">
                                    Continuar comprando
                                </a>
                            </div>
                            <div class="col">
                                <a href="{{ INCLUDE_PATH }}carrinho"
                                class="btn btn-success btn-4 w-100">
                                    Ir para o carrinho
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Page header -->
    <div class="page-header d-print-none">
        <div class="container-xl">
            <div class="row g-2 align-items-center">
                <div class="col">
                    <h2 class="page-title text-ankara sub-titulo">Produtos</h2>
                </div>
                <div class="col-auto ms-auto d-print-none">
                    <form method="GET" class="d-flex" action="/produtos">
                        <div class="me-3">
                            <div class="input-icon">
                                <input type="text"
                                       name="busca"
                                       value="{{ busca|e }}"
                                       class="form-control form-control-ankara"
                                       placeholder="Pesquisar…">
                                <span class="input-icon-addon">
                                    <svg xmlns="http://www.w3.org/2000/svg"
                                         width="24" height="24"
                                         viewBox="0 0 24 24"
                                         fill="none"
                                         stroke="#2c3e50"
                                         stroke-width="2"
                                         stroke-linecap="round"
                                         stroke-linejoin="round"
                                         class="icon icon-1">
                                        <path d="M10 10m-7 0a7 7 0 1 0 14 0a7 7 0 1 0 -14 0"></path>
                                        <path d="M21 21l-6 -6"></path>
                                    </svg>
                                </span>
                            </div>
                        </div>
                        <button type="submit" class="btn btn-ankara">
                            Buscar
                        </button>
                    </form>
                </div>
            </div>
        </div>
    </div>

    <!-- Page body -->
    <div class="page-body">
        <div class="container-xl mt-3">
            <div class="row g-4">

                {% if produtos %}
                    <div class="col-md-12">
                        <div class="row row-cards">
                            {% for produto in produtos %}
                                {% set imagemUrl = produto.imagem is not empty 
                                    ? (INCLUDE_PATH ~ "files/produtos/" ~ produto.id ~ "/" ~ produto.imagem)|replace({' ': '%20'}) 
                                    : INCLUDE_PATH ~ "assets/preview-image/product.jpg" 
                                %}
                                {% set precoFormatado = produto.preco
                                    |number_format(2, ',', '.') %}

                                <div class="col-sm-6 col-lg-4">
                                    <div class="card card-ankara">
                                        <a href="{{ INCLUDE_PATH }}p/{{ produto.link }}" class="d-block">
                                            <img src="{{ imagemUrl }}"
                                                 class="card-img-top"
                                                 alt="{{ produto.titulo }}">
                                        </a>
                                        <div class="card-body">
                                            <div class="d-flex align-items-center">
                                                <div class="w-100">
                                                    <h2 class="mb-0">{{ produto.titulo }}</h2>
                                                    <h3>{{ produto.codigo_produto }}</h3>
                                                    <h2 class="text-red">
                                                        R$ {{ precoFormatado }}
                                                    </h2>
                                                    <button 
                                                        class="btn btn-yellow fw-bold btn-pill w-100 add-to-cart"
                                                        data-produto-id="{{ produto.id }}"
                                                        data-produto-estoque="{{ produto.estoque }}"
                                                    >
                                                        {% if produto.estoque %}
                                                        Adicionar ao carrinho
                                                        {% else %}
                                                        Encomendar
                                                        {% endif %}
                                                    </button>
                                                </div>
                                            </div>
                                            {% if not produto.estoque %}
                                            <p class="mt-2 text-center text-secondary">Produzido especialmente para você! Início da produção após a confirmação do pagamento.</p>
                                            {% endif %}
                                        </div>
                                    </div>
                                </div>
                            {% endfor %}
                        </div>
                    </div>

                    <!-- Paginação personalizada para Ankara -->
                    <div class="col-12">
                        <div class="d-flex align-items-center justify-content-center">
                            <div class="pagination-ankara">
                                {{ pagination_html|raw }}
                            </div>
                        </div>
                    </div>
                {% else %}
                    <div class="col-12">
                        <div class="alert alert-warning w-100" role="alert">
                            <div class="d-flex">
                                <div class="alert-icon">
                                    <svg xmlns="http://www.w3.org/2000/svg"
                                         width="24" height="24"
                                         viewBox="0 0 24 24"
                                         fill="none"
                                         stroke="#2c3e50"
                                         stroke-width="2"
                                         stroke-linecap="round"
                                         stroke-linejoin="round"
                                         class="icon alert-icon icon-2">
                                        <path d="M3 12a9 9 0 1 0 18 0a9 9 0 0 0 -18 0"></path>
                                        <path d="M12 9h.01"></path>
                                        <path d="M11 12h1v4h1"></path>
                                    </svg>
                                </div>
                                <div>
                                    <h4 class="alert-title">Nenhum Produto (Ankara)</h4>
                                    <div class="text-secondary">
                                        O tema Ankara não encontrou produtos para exibir.
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                {% endif %}

            </div>
        </div>
    </div>
{% endblock %}

{# ----------- Script para adicionar ao carrinho via AJAX ----------- #}
<script>
    $(document).ready(function() {
        $(".add-to-cart").on("click", function(e) {
            e.preventDefault();

            var produtoId = $(this).data("produto-id");
            var produtoEstoque = $(this).data("produto-estoque");
            // Neste exemplo, fixamos quantidade = 1
            var quantidade = 1;

            $.ajax({
                url: "{{ INCLUDE_PATH }}back-end/carrinho/adicionar.php",
                method: "POST",
                dataType: 'json',
                data: {
                    produto_id: produtoId,
                    quantidade: quantidade
                },
                success: function(response) {
                    if (response.status === "sucesso") {
                        // Atualiza badge do carrinho
                        $("#cart-count")
                            .text(response.numero_itens > 9 ? '9+' : response.numero_itens)
                            .show();
                        // Exibe modal de sucesso
                        var myModal = new bootstrap.Modal(
                            document.getElementById('modal-success')
                        );
                        document.getElementById("modalMsg").textContent = 'O produto foi adicionado ao seu carrinho.';
                        myModal.show();
                    } else {
                        // Exibe modal de erro
                        var myErrorModal = new bootstrap.Modal(
                            document.getElementById('modal-error')
                        );
                        myErrorModal.show();
                    }
                },
                error: function(xhr, status, error) {
                    console.error("Erro na requisição AJAX: " + error);
                    // Opcional: exibir modal de erro genérico
                    var myErrorModal = new bootstrap.Modal(
                        document.getElementById('modal-error')
                    );
                    myErrorModal.show();
                }
            });
        });
    });
</script>